// ---------------------------------------------------------------------------
// Configuration for the demo video. Provide either a locally hosted file
// (localSrc, e.g. a file placed in /public/videos) or a YouTube ID — localSrc
// takes priority if both are set. Leave both blank to show a styled "video
// coming soon" placeholder instead of a broken embed.
// ---------------------------------------------------------------------------

export const demoVideo = {
  localSrc: '/videos/demo.mp4', // e.g. '/videos/demo.mp4', or '' to use YouTube/placeholder instead
  youtubeId: '', // TODO: e.g. 'Ag4i_cpIoRk'
  title: 'How WarmBox works, in under a minute',
  caption: 'A quick look at the pour-water activation, from unboxing to a hot meal.',
}
